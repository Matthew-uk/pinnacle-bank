import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  images: {
    domains: ['oriontrustbank.com', 'cdn.dribbble.com', 'arttrk.com'], // Add your image domain here
  },
};

export default nextConfig;
